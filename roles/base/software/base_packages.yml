- name: install base packages
  package:
    name:
      - inxi
      - curl
      - samba
      - restic
      - zsh
      - syncthing

- name: set up zsh for user
  become: no
  vars:
    the_user: "{{ ansible_user_id }}"


